meta:
  name: "libisl smoke test"
  harbours:
     - libisl

tasks:
  - boot
  - command:
      args: "cd /tests/libisl"
  - command:
      args: "from max.pip | ./isl_pip --format=set --context=gbr -T"
